package compiler;

public class Scanner {
	
	
	static final 
	
	/** 
	 * Proposed Interface to Parser (see Wirth):
	 * 
	 * Method get represents the actual scanner.
	 * write token in to global variable ident (integer wahrscheinlich)
	 * 
	 * Probleme:	-> Positiontracking fuer error handling
	 * 				-> ident muss type und auch eventuelle values ubergeben
	 * 				-> 
	 * 
	 * Vorschlag:
	 * 				entweder ident als objekt (klasse) anlegen mit den zugehoerigen Werten
	 * 				oder mehrere globale Integer oder was auch immer Variablen die Typ, value, position, .. angeben
	 * 
	 * soweit der Status
	 * 
	 */
	public static get(){
		
		return token;
	}
	
	

}
